"use client";
import{useState}from"react";import{signIn}from"next-auth/react";import{useRouter}from"next/navigation";import{Dumbbell}from"lucide-react";
export default function AuthForm(){const[mode,setMode]=useState<"login"|"signup">("login"),[error,setError]=useState(""),[busy,setBusy]=useState(false);const router=useRouter();async function submit(formData:FormData){setBusy(true);setError("");const email=String(formData.get("email")||""),password=String(formData.get("password")||"");try{if(mode==="signup"){const response=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:formData.get("name"),email,password})});const body=await response.json();if(!response.ok)throw new Error(body.error||"Could not create account.")}const result=await signIn("credentials",{email,password,redirect:false});if(result?.error)throw new Error(mode==="login"?"Email or password is incorrect.":"Account created, but sign-in failed.");router.push("/");router.refresh()}catch(value){setError(value instanceof Error?value.message:"Something went wrong.")}finally{setBusy(false)}}return <main className="login"><section><div className="onboardingBrand"><Dumbbell/><b>REPS</b></div><div className="authTabs"><button type="button" className={mode==="login"?"active":""} onClick={()=>{setMode("login");setError("")}}>Sign in</button><button type="button" className={mode==="signup"?"active":""} onClick={()=>{setMode("signup");setError("")}}>Create account</button></div><p className="eyebrow">{mode==="login"?"WELCOME BACK":"START TRAINING"}</p><h1>{mode==="login"?<>Ready to<br/><em>move?</em></>:<>Join the<br/><em>challenge.</em></>}</h1><p className="lede">{mode==="login"?"Your challenges are waiting.":"Create an account and track every set across devices."}</p><form action={submit}>{mode==="signup"&&<label>Name<input name="name" autoComplete="name" minLength={2} required/></label>}<label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete={mode==="login"?"current-password":"new-password"} minLength={mode==="login"?8:12} required/></label>{error&&<p className="formError" role="alert">{error}</p>}<button className="primary" disabled={busy}>{busy?"Please wait…":mode==="login"?"Sign in":"Create account"}</button></form><small>{mode==="login"?"New here? Choose Create account above.":"Passwords must be at least 12 characters."}</small></section></main>}