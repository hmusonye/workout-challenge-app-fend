import{NextResponse}from"next/server";import{auth}from"@/auth";import{getSql}from"@/db/client";import{unauthorized}from"@/lib/api";export async function GET(){const s=await auth();if(!s)return unauthorized();return NextResponse.json(await getSql()`select id,entity_type,entity_id,action,before_data,after_data,occurred_at,recorded_at,request_id from audit_events where user_id=${s.user.id} order by recorded_at desc limit 500`)}
